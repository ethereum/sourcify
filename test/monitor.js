describe('monitor', function(){
  it.skip('getByteCodeMetadataAndSources: gets all sources from IPFS by contract address');

  it.skip('retrieve code (an E2E test)', function(){
    // Entire process is automated - no submission necessary?
    // 1. Launch testrpc
    // 2. Launch monitor, watching localhost
    // 3. Launch local ipfs node --offline
    // 4. Deploy contracts to testrpc
    // 5. Publish contracts to local ipfs node
    // 6. Verify address is in addressDB
    // 7. Verify published to repository
  })
});
