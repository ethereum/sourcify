{
  "name": "ui",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "parcel serve -p 1234 index.html",
    "start:prod": "cross-env NODE_ENV=production parcel index.html",
    "start:dev": "cross-env NODE_ENV=development parcel index.html",
    "start:test": "cross-env NODE_ENV=testing parcel index.html",
    "build": "rimraf dist && parcel build index.html",
    "build:env": "cross-env NODE_ENV=production parcel build index.html"
  },
  "dependencies": {
    "react": "^16.12.0",
    "react-dom": "^16.12.0",
    "react-dropzone": "^10.2.1",
    "react-select": "^3.0.8"
  },
  "devDependencies": {
    "@babel/preset-env": "^7.8.3",
    "@babel/preset-react": "^7.8.3",
    "cross-env": "^6.0.3",
    "parcel-bundler": "^1.12.4",
    "rimraf": "^3.0.1"
  }
}
