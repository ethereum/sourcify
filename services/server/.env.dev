# Server config

#################################
### Required variables start ####
#################################

# Sourcify's database postgres connection
SOURCIFY_POSTGRES_HOST="localhost"
SOURCIFY_POSTGRES_DB="sourcify"
SOURCIFY_POSTGRES_USER="sourcify"
SOURCIFY_POSTGRES_PASSWORD="sourcify"
SOURCIFY_POSTGRES_PORT=5432
# SOURCIFY_POSTGRES_SCHEMA=custom-schema # by default "public" is used
# SOURCIFY_POSTGRES_MAX_CONNECTIONS=5 # Default set to 15
# SOURCIFY_POSTGRES_SSL=true # Enable SSL connection. Default: false
# SOURCIFY_POSTGRES_SSL_REJECT_UNAUTHORIZED=false # Reject self-signed certificates. Default: false (allows self-signed certs)

###############################
### Required variables end ####
###############################

######################################
### Public env variables (optional) ##
######################################

### UI URLS, required to redirect to them from the server
# Verify UI, e.g. a deployed instance of https://github.com/sourcifyeth/verify.sourcify.dev
SOURCIFY_VERIFY_UI=
# Repo UI, e.g. a deployed instance of https://github.com/sourcifyeth/repo.sourcify.dev 
# or for self-hosted Sourcify instances could be an explorer that follows the same
# path structure like /{chainId}/{address}
SOURCIFY_REPO_UI=

IPFS_GATEWAY=https://ipfs.io/ipfs/
# Pass additional headers to the request (e.g. { 'x-authentication': 'token' } )
IPFS_GATEWAY_HEADERS=

# Won't save files in IPFS MFS if empty
IPFS_API=

# Set to "production" for production builds and "development" for local development.
NODE_ENV=
# Used to set the config file to use. See src/config. Default sourcify uses "master" and "staging".
NODE_CONFIG_ENV=
# Overrides the log level. Normally, if NODE_ENV production set to "info", otherwise "debug". Values can be silly, debug, info, warn, error
NODE_LOG_LEVEL=

# Configuration for the Piscina worker pool
# How long one worker can be idle before it is shutdown in seconds. If not set, uses 30 s as default.
WORKER_IDLE_TIMEOUT=
# How many verifications one worker can handle concurrently. If not set, uses 5 as default.
CONCURRENT_VERIFICATIONS_PER_WORKER=

# How long a single compilation may run, in milliseconds, before the compiler is killed
# and the verification fails with compiler_timeout. Applies to solc, vyper and fe.
# If not set, uses 45 minutes as default.
COMPILER_TIMEOUT_MS=

######################################
## Private env variables (optional) ##
######################################

# Verifier Alliance database postgres connection
# # ALLIANCE_POSTGRES_SCHEMA=custom-schema # by default "public" is used
# Option 1: Use postgres
# ALLIANCE_POSTGRES_HOST=
# ALLIANCE_POSTGRES_DB=
# ALLIANCE_POSTGRES_USER=
# ALLIANCE_POSTGRES_PASSWORD=
# ALLIANCE_POSTGRES_PORT=
# Option 2: Use Google Cloud SQL connector
# ALLIANCE_GOOGLE_CLOUD_SQL_INSTANCE_NAME=
# ALLIANCE_GOOGLE_CLOUD_SQL_DATABASE=
# ALLIANCE_GOOGLE_CLOUD_SQL_USER=
# ALLIANCE_GOOGLE_CLOUD_SQL_PASSWORD=
# ALLIANCE_DB_MAX_CONNECTIONS=5 # Default set to 15

# Simple authentication token for the /private/** endpoints
SOURCIFY_PRIVATE_TOKEN=

# Ethereum JSON-RPC Providers for different chains.
# Will throw if missing for a chain.
DRPC_API_KEY=
ALCHEMY_API_KEY=
QUICKNODE_API_KEY=
# If you are using different subdomains (endpoints) for different enviroments (staging, production, etc)
QUICKNODE_SUBDOMAIN=
# Optional, if not set will use ALCHEMY_API_KEY
ALCHEMY_API_KEY_OPTIMISM=
# Optional, if not set will use ALCHEMY_API_KEY
ALCHEMY_API_KEY_ARBITRUM=

# Optional, Needed for the Import from Etherscan functionality and for fetching the contract creation information.
# If no specialized key for a chain is set, ETHERSCAN_API_KEY will be used.
# This allows to use Etherscan API v2 with the same key for all chains, or to use a different key for each chain if wanted.
ETHERSCAN_API_KEY=
ETHERSCAN_API_KEY_MAINNET=
ETHERSCAN_API_KEY_ARBITRUM=
ETHERSCAN_API_KEY_ARBITRUM_NOVA=
ETHERSCAN_API_KEY_POLYGON=
ETHERSCAN_API_KEY_ZKEVM_POLYGON=
ETHERSCAN_API_KEY_BSC=
ETHERSCAN_API_KEY_CELO=
ETHERSCAN_API_KEY_MOONBEAM=
ETHERSCAN_API_KEY_MOONRIVER=
ETHERSCAN_API_KEY_GNOSIS=
ETHERSCAN_API_KEY_OPTIMISM=
ETHERSCAN_API_KEY_BASE=
ETHERSCAN_API_KEY_SCROLL=
ETHERSCAN_API_KEY_FRAXTAL=
ETHERSCAN_API_KEY_AVALANCHE=
ETHERSCAN_API_KEY_MANTLE=
ETHERSCAN_API_KEY_LINEA=
ETHERSCAN_API_KEY_BLAST=
ETHERSCAN_API_KEY_WORLDCHAIN=
ETHERSCAN_API_KEY_SONIC=
ETHERSCAN_API_KEY_TAIKO=
ETHERSCAN_API_KEY_SEI=

# Optional, needed for the verification on EtherscanApiVerify storage service
BLOCKSCOUT_API_KEY=
ROUTESCAN_API_KEY=

# For fetching creation transactions in Binance Smart Chain's explorer, since Etherscan's free tier is turned off for BSC. 
NODEREAL_API_KEY=

# Required for fetching contract creation information using the VeChain Stats API
VECHAIN_STATS_API_KEY=

# Authentication for ethpandaops nodes
CF_ACCESS_CLIENT_ID=
CF_ACCESS_CLIENT_SECRET=

# Optional, project id for Google Cloud, used for log tracing.
GOOGLE_CLOUD_PROJECT=

# Optional, S3 configuration for RepositoryS3Service
S3_BUCKET=
S3_REGION=
S3_ACCESS_KEY_ID=
S3_SECRET_ACCESS_KEY=
S3_ENDPOINT=

# Optional, ArDrive Turbo configuration for TurboRepositoryService.
# Enable it by adding TurboRepository to storage.writeOrWarn in src/config.
# Arweave JWK as a JSON string. This is a wallet key: keep it out of the image.
TURBO_PRIVATE_KEY=
# Only "arweave" is supported, which is the default. Leave it unset.
TURBO_TOKEN=
# Defaults to the Turbo production upload service
TURBO_UPLOAD_SERVICE_URL=
# Gateway URL recorded and logged for uploaded files. Not where they are
# uploaded. Defaults to the gateway the upload service names as its own.
TURBO_GATEWAY_URL=
# App-Name tag set on every uploaded file. Defaults to Sourcify
TURBO_APP_NAME=
# Milliseconds before an upload is aborted. Defaults to 60000
TURBO_UPLOAD_TIMEOUT=
# Warn at startup when the balance is at or below this many winston credits.
# Defaults to 0, i.e. warn only when there is nothing left to spend. Uploads
# above the free-tier ceiling fail once the balance is gone, and a failed
# upload is only warned about, so give yourself a margin here.
# Chain ids to archive, comma separated. Empty archives every chain.
# Arweave is paid for per byte and permanent, so this is the setting that
# decides what the backend costs. Sourcify verifies far more on some chains
# than others, so scope this deliberately rather than leaving it open.
TURBO_CHAIN_IDS=
# Match qualities to archive: full, partial, or both. Empty archives both.
TURBO_MATCH_QUALITIES=
TURBO_MIN_BALANCE_WINC=

# Optional, S3 configuration for storing failed verification inputs for debugging purposes
DEBUG_DATA_S3_BUCKET=
DEBUG_DATA_S3_REGION=
DEBUG_DATA_S3_ACCESS_KEY_ID=
DEBUG_DATA_S3_SECRET_ACCESS_KEY=
DEBUG_DATA_S3_ENDPOINT=
